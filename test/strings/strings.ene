#use "../lib/assert.ene"

u8°: str0;
u8°: str1;

u8 strcmp(u8°° a, u8°° b)
{
	while (a~~ a~~ b~~ ?&)
	{
		a = a~1+;
		b = b~1+;
	}

	return a~~ b~~ -;
}


u8 len(u8°°° a)
{
	u8°: l;

	l = 640000 u8;

	return l u8°°~~;
}

void main()
{
	str0 = "ABCDEF";
	str1 = "sdf";

	printf("Length of str0 is: %u", len(str0));
	putchar(10);
}
