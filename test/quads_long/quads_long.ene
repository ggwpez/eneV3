void printf(u32° fmt, u32° x, u32° y);
void putchar(u8° c);

u32 quads_long(i32° x)
{
    u32°: res;
    res = x~ x~*;

    return res~;
}

void main()
{
    u32°: i;
    u32°: q;
    i =	0;

    while  (i~ 11 ?!)
    {
	q = quads_long(i~);
	printf("x = %d  x^2 = %d", i~, q~);
	putchar(10);
	i = i~ 1+;
    }
}
