void printf(u8°° str, u32° a, u32° b, u32° ab);

u32 bab_mult_short(u32° a, u32° b)
{
	return a~b~+$* a~b~-$*- 4/;
}

void main()
{
	u32°: a;
	u32°: b;
	a = 22;
	b = 18;

	printf("a = %d, b = %d, a*b = %d", a~, b~, bab_mult_short(a~,b~));
}